import type { Member } from "../../types/memberTypes";

export default function Card({nome, rm, turma, foto}: Member) {
    return(
        <div>
            <div>
                
            </div>
        </div>
    );
}